install.packages('RMySQL', type = 'source')
ls
library(RMySQL)
library(psych)
con <- dbConnect(MySQL(),
    user = 'RDSUser',
    password = 'YourPass',
    host = 'RDS Host',
    dbname='YourDB')
q()
